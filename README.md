# 🚢 Морской бой — Telegram Mini App

Классический морской бой как Telegram Mini App на чистом HTML5 + Vanilla JS + Node.js + Socket.io.

---

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск локально
```bash
npm run dev
```
Откроется на `http://localhost:3000`

### 3. Для разработки (без сервера, только бот)
Просто открой `public/index.html` в браузере — игра с ботом работает без сервера.

---

## 📁 Структура проекта

```
battleship/
├── public/              ← Frontend (статика)
│   ├── index.html       ← Главный HTML + структура экранов
│   ├── game.js          ← Вся игровая логика
│   └── style.css        ← Стили (адаптив + TG темы)
├── data/                ← Автосоздаётся, хранит SQLite БД
│   └── game.db          ← База данных игроков и игр
├── server.js            ← Node.js + Express + Socket.io
├── package.json
├── vercel.json          ← Конфиг для Vercel
└── README.md
```

---

## 🎮 Режимы игры

| Режим         | Описание                                    |
|---------------|---------------------------------------------|
| 🤖 Бот (лёгкий) | Случайные выстрелы                         |
| 🤖 Бот (средний)| Охота + добивание                          |
| 🤖 Бот (сложный)| Шахматный паттерн + умное добивание        |
| 🌊 Случайный  | Онлайн PvP через matchmaking               |
| 👥 С другом   | Онлайн PvP по invite-ссылке                |

---

## 🌍 Деплой на Vercel

```bash
# Установи Vercel CLI
npm i -g vercel

# Деплой
vercel deploy --prod
```

> ⚠️ Vercel — serverless, WebSocket работает не идеально.
> Для онлайна лучше использовать Railway или Render.

---

## 🚂 Деплой на Railway (рекомендуется для WebSocket)

1. Создай аккаунт на [railway.app](https://railway.app)
2. `New Project → Deploy from GitHub`
3. Добавь переменные окружения:
   ```
   PORT=3000
   CORS_ORIGIN=*
   ```
4. Готово! Railway автоматически поддерживает WebSocket.

---

## 🤖 Регистрация Telegram Bot

1. Напиши [@BotFather](https://t.me/BotFather) в Telegram
2. Создай бота: `/newbot`
3. Создай Mini App: `/newapp`
4. Укажи URL твоего деплоя
5. Получишь ссылку вида `t.me/your_bot/app`

---

## ⚙️ Переменные окружения

| Переменная   | По умолчанию      | Описание                   |
|-------------|-------------------|----------------------------|
| `PORT`      | `3000`            | Порт сервера               |
| `DB_PATH`   | `./data/game.db`  | Путь к SQLite базе         |
| `CORS_ORIGIN`| `*`              | Разрешённые CORS origins   |

---

## 📡 REST API

| Endpoint                  | Описание                    |
|--------------------------|------------------------------|
| `GET /api/leaderboard`   | Топ-50 игроков по победам   |
| `GET /api/stats/:id`     | Статистика игрока по TG ID  |
| `GET /api/status`        | Статус сервера              |

---

## 🔌 WebSocket Events

### Client → Server
```json
{ "type": "matchmake", "mode": "random", "playerId": "...", "playerName": "..." }
{ "type": "place_ships", "field": [[...]], "roomId": "..." }
{ "type": "shoot", "roomId": "...", "r": 3, "c": 5 }
```

### Server → Client
```json
{ "type": "matched", "roomId": "...", "opponent": { "id": "...", "name": "..." } }
{ "type": "enemy_ready" }
{ "type": "turn", "roomId": "...", "isMyTurn": true }
{ "type": "shot_result", "r": 3, "c": 5, "hit": true, "sunk": false, "gameOver": false }
```

---

## 🛠️ Дальнейшее развитие

- [ ] Чат во время игры
- [ ] Турниры (brackets)
- [ ] Достижения и бейджи
- [ ] Реплей последней игры
- [ ] Стрик побед
- [ ] Push-уведомления через TG Bot API
- [ ] Кастомные темы оформления
